.navbar-right(ng-controller="LoginCtrl")
    form.navbar-form(ng-hide='identity.isAuthenticated()')
        .form-group
            input.form-control(placeholder='Email', ng-model='email', type='email')
        .form-group
            input.form-control(placeholder='Password', ng-model='password',type='password')
        button.btn.btn-primary(ng-click="signin()",id='login') Signin